<div class="container-fluid">
<%= render "shared/banner" %>
</div>
<br>
  <div>
    <br>
    <h1>Available Buddies</h1>
      <br>
      <br>
      <div class='container'>
        <div class="row">
          <% @buddies.each do |buddy| %>
            <div class="col-12 col-md-6 col-lg-3">
              <%= render "shared/buddy_card", buddy: buddy %>
            </div>
          <% end %>
        </div>
      </div>
      <br>

      <%= link_to "Your bookings", bookings_path, class: "btn btn-outline-info" %>
      <%= link_to "Add your buddy", new_buddy_path, class: "btn btn-outline-primary" %>
    </div>
</div>
