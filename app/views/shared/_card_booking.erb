<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8-sm-6">
      <div class="buddy_card">
        <div class="product-img"style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path booking.buddy.photo.key, height: 300, width: 400, :quality=>"auto", :fetch_format=>:auto %>')">
        </div>

        <div class="card-description">
          <p><%= booking.buddy.name %> is an awesome fella. This <%= booking.buddy.category %> has thus has been booked by you </h2>
          <p>From: <%= booking.start_on.strftime("%a %b %e at %l:%M%p") %> - To: <%= booking.end_on.strftime("%a %b %e at %l:%M%p") %></p>
          <div class="p-2">
            <%= link_to "back", bookings_path, class: "btn btn-outline-primary" %>
          </div>
        </div>


      </div>
    </div>
  </div>
</div>


